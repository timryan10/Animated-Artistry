import { useContext, useState } from "react"
import { useNavigate } from "react-router"

function Auth() {

    return (
        <div>This is where user will sign in to make updates</div>
    )
}

export default Auth